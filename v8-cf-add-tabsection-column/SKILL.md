---
name: v8-cf-add-tabsection-column
user-invocable: true
description: >
  Добавление колонки (реквизита) в табличную часть справочника в JSON-исходниках распакованной конфигурации 1С.
  Читает определение колонки из эталонного каталога и вставляет её в Catalog.json целевого справочника.
---

# v8-cf-add-tabsection-column

## Описание

Скилл добавляет колонку в табличную часть справочника в исходниках распакованной конфигурации 1С (v8unpack-формат).

- Добавляет запись колонки в список колонок табличной части (`ts_entry[2]`)
- Увеличивает счётчик колонок в `ts_entry[2][1]`
- Перезаписывает `Catalog.tabular_sections.json`

## Использование

- «добавь колонку Реквизит1Строка10 в табличную часть ТабличнаяЧасть1 справочника Справочник01»
- «add column to tabular section Справочник01.ТабличнаяЧасть1»

## Скрипт

Файл: `tools/add_tabsection_column.py`

```
python tools/add_tabsection_column.py <source_dir> <catalog_name> <ts_name> <col_name> <col_source_catalog_dir>
```

Аргументы:
- `source_dir` — путь к распакованным исходникам проекта (`src/<Проект>`)
- `catalog_name` — имя папки справочника (например, `Справочник01`)
- `ts_name` — имя табличной части (например, `ТабличнаяЧасть1`)
- `col_name` — имя добавляемой колонки (например, `Реквизит1Строка10`)
- `col_source_catalog_dir` — папка с эталонным `Catalog.json`

## Определение source_dir

Путь к исходникам берётся из `v8.projects.json`:

- Топ-уровневый проект (например, `BAU`): `projects.BAU.src` = `src/BAU`
- Подпроект (например, `BAU_KlientBank`): `projects.BAU.projects.BAU_KlientBank.src` = `src/BAU/BAU_KlientBank`

## Требования

- Исходники должны быть предварительно распакованы через v8unpack
- Если табличная часть не найдена: ответить «Ошибка: табличная часть <ts_name> не найдена в справочнике <catalog_name>.»
- Если колонка не найдена в эталоне: ответить «Ошибка: колонка <col_name> не найдена в <col_source_catalog_dir>.»

## Связанные скиллы

- v8-cf-remove-tabsection-column — удаление колонки из табличной части
- v8-cf-add-tabular-section — добавление табличной части в справочник
